---
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/AdminFormsList.tsx
  - src/components/AdminFormSubmissions.tsx
autonomous: true
requirements:
  - TABLE-01
  - LOAD-01
  - LOAD-02
---

# Plan 12-01: Sticky Table Headers & Skeleton Shimmer Loaders

## Goal
Implement sticky table headers across admin tables with light/dark adaptive solid backgrounds (TABLE-01), and replace static loading spinners/text with skeleton shimmer placeholders in `/admin/forms` (LOAD-01) and `/admin/forms/[id]/submissions` (LOAD-02).

## Tasks

<task id="1" name="css_sticky_headers_and_shimmer" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Add sticky table header rules with solid background and theme adaptation for light and dark modes to .forms-dashboard-table thead th. Enhance .admin-skeleton-line and add .admin-skeleton-card with smooth 60fps CSS shimmer keyframe animations in src/app/globals.css.
  </action>
  <acceptance_criteria>
    src/app/globals.css includes sticky styling (position: sticky; top: 0; z-index: 10) with opaque theme-adaptive backgrounds for table headers, plus enhanced skeleton shimmer animation styles.
  </acceptance_criteria>
</task>

<task id="2" name="admin_forms_list_skeleton_loading" type="auto">
  <read_first>
    <file>src/components/AdminFormsList.tsx</file>
  </read_first>
  <action>
    Update src/components/AdminFormsList.tsx to replace the static loading empty-state message with animated skeleton shimmer rows in the table when loading is true.
  </action>
  <acceptance_criteria>
    src/components/AdminFormsList.tsx renders placeholder skeleton rows (with shimmer animation) matching the table structure during data fetch.
  </acceptance_criteria>
</task>

<task id="3" name="admin_form_submissions_skeleton_loading" type="auto">
  <read_first>
    <file>src/components/AdminFormSubmissions.tsx</file>
  </read_first>
  <action>
    Update src/components/AdminFormSubmissions.tsx to show a comprehensive skeleton shimmer view (stats and card list) during initial loading (loading && !data) and ensure sticky header ergonomics work seamlessly for table views.
  </action>
  <acceptance_criteria>
    src/components/AdminFormSubmissions.tsx renders animated skeleton cards/shimmer components during loading without plain text empty-state blockers.
  </acceptance_criteria>
</task>

<task id="4" name="verify_build_and_lint" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
    <file>src/components/AdminFormsList.tsx</file>
    <file>src/components/AdminFormSubmissions.tsx</file>
  </read_first>
  <action>
    Run TypeScript type-checking and ESLint to verify there are no compilation errors or lint issues.
  </action>
  <acceptance_criteria>
    npx tsc --noEmit and npm run lint pass with 0 errors.
  </acceptance_criteria>
</task>

## Verification
- Run `npx tsc --noEmit --pretty false` to ensure type integrity.
- Run `npm run lint` to ensure code cleanliness.
- Verify sticky header CSS rules and skeleton shimmer DOM elements.

## must_haves
- truths:
  - "Table headers stay sticky at top during vertical scrolling with solid backgrounds in light and dark mode"
  - "Admin forms list displays skeleton shimmer rows while loading"
  - "Admin form submissions displays skeleton shimmer loaders while fetching data"
- prohibitions:
  - "Do not introduce heavy third-party animation libraries (use pure CSS)"

## Artifacts this phase produces
- File: `src/app/globals.css`
- File: `src/components/AdminFormsList.tsx`
- File: `src/components/AdminFormSubmissions.tsx`
