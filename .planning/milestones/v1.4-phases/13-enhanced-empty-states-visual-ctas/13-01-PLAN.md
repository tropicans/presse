---
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/AdminFormsList.tsx
  - src/components/AdminFormSubmissions.tsx
autonomous: true
requirements:
  - EMPTY-01
  - EMPTY-02
---

# Plan 13-01: Enhanced Empty States & Visual CTAs

## Goal
Implement minimalist SVG illustrations, contextual guidance, and actionable Call-to-Action (CTA) buttons for empty data states across the Admin dashboard (`/admin/forms` and `/admin/forms/[id]/submissions`), fulfilling requirements EMPTY-01 and EMPTY-02.

## Tasks

<task id="1" name="empty_states_css_styling" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Add CSS styling for enhanced empty states in src/app/globals.css:
    - .admin-empty-state-card / .forms-dashboard-empty-state: centered flex/grid layout, generous padding, max-width typography.
    - .admin-empty-icon-wrap: circular icon container with soft accent background (theme-adaptive for light and dark mode), subtle scale micro-animation on hover.
    - .admin-empty-state-actions: flex wrap container for CTA buttons with consistent gap.
  </action>
  <acceptance_criteria>
    src/app/globals.css includes .admin-empty-icon-wrap and .admin-empty-state-actions styles with dark mode variables and clean responsive layout.
  </acceptance_criteria>
</task>

<task id="2" name="admin_forms_list_empty_state" type="auto">
  <read_first>
    <file>src/components/AdminFormsList.tsx</file>
  </read_first>
  <action>
    Update src/components/AdminFormsList.tsx:
    - In the forms.length === 0 state (EMPTY-01): render a minimalist document/form SVG illustration, heading "Belum Ada Formulir", informative guidance message, and a prominent CTA button "Buat Formulir Baru" that invokes handleOpenCreateModal.
    - In the filteredForms.length === 0 state: render a minimalist search SVG illustration, heading "Tidak Ada Form yang Cocok", and an action button to reset search and status filter.
  </action>
  <acceptance_criteria>
    When forms.length === 0, /admin/forms displays an SVG illustration and a "Buat Formulir Baru" button that opens the form creation modal.
  </acceptance_criteria>
</task>

<task id="3" name="admin_form_submissions_empty_state" type="auto">
  <read_first>
    <file>src/components/AdminFormSubmissions.tsx</file>
  </read_first>
  <action>
    Update src/components/AdminFormSubmissions.tsx:
    - In the data.totalItems === 0 state (EMPTY-02): render a minimalist inbox/submission SVG illustration, heading "Belum Ada Kiriman Masuk", and contextual guidance (e.g. link to copy public URL if PUBLISHED, or link to publish if DRAFT).
    - In the filteredItems.length === 0 state: render a minimalist search SVG illustration, heading "Tidak Ada Kiriman yang Cocok", and an action button to clear search and reset filters.
  </action>
  <acceptance_criteria>
    When data.totalItems === 0, /admin/forms/[id]/submissions displays an SVG illustration and actionable guidance for collecting submissions.
  </acceptance_criteria>
</task>

<task id="4" name="verify_build_and_lint" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
    <file>src/components/AdminFormsList.tsx</file>
    <file>src/components/AdminFormSubmissions.tsx</file>
  </read_first>
  <action>
    Run TypeScript type-checking and ESLint to verify there are no compilation errors or lint issues.
  </action>
  <acceptance_criteria>
    npx tsc --noEmit and npm run lint pass with 0 errors.
  </acceptance_criteria>
</task>

## Verification
- Run `npx tsc --noEmit --pretty false` to verify TypeScript types.
- Run `npm run lint` to verify code quality.
- Verify empty state rendering on `/admin/forms` and `/admin/forms/[id]/submissions`.

## must_haves
- truths:
  - "When no forms exist on /admin/forms, user sees a minimalist SVG illustration and a Buat Formulir Baru CTA button"
  - "When no submissions exist on /admin/forms/[id]/submissions, user sees a minimalist SVG illustration and informative guidance"
  - "Filtered empty states provide clean search illustrations and reset actions"
- prohibitions:
  - "Do not introduce heavy external illustration or icon packages (use lightweight inline SVGs)"

## Artifacts this phase produces
- File: `src/app/globals.css`
- File: `src/components/AdminFormsList.tsx`
- File: `src/components/AdminFormSubmissions.tsx`
