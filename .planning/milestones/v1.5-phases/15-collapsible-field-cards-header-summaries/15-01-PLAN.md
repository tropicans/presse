---
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/AdminFormEditor.tsx
autonomous: true
requirements:
  - BUILDER-03
  - BUILDER-04
  - BUILDER-05
---

# Plan 15-01: Collapsible Field Cards & Header Summaries

## Goal
Implement collapsible field cards with interactive header toggles and event isolation (BUILDER-03), compact informative header summaries including index number, question label, field type pill, required badge, and option/score counts (BUILDER-04), and mass toggle controls ("Buka Semua" and "Tutup Semua") (BUILDER-05).

## Tasks

<task id="1" name="css_collapsible_cards_and_summaries" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Add CSS styles in src/app/globals.css for collapsible card states, interactive clickable card headers with rotate transition chevron icons, compact summary badges (order index, type pill, required badge, option count badge), and mass action toggle links.
  </action>
  <acceptance_criteria>
    src/app/globals.css includes styles for `.admin-builder-card.collapsed`, `.admin-builder-card-head.clickable`, `.admin-builder-chevron`, `.admin-builder-header-summary`, `.admin-builder-mass-actions`, and dark mode adaptations.
  </acceptance_criteria>
</task>

<task id="2" name="admin_form_editor_collapsible_cards" type="auto">
  <read_first>
    <file>src/components/AdminFormEditor.tsx</file>
  </read_first>
  <action>
    1. Add `collapsedFieldIds` state (`Set<string>`) in `AdminFormEditor.tsx`.
    2. Add `toggleCollapse(fieldId)` to toggle an individual card.
    3. Add `expandAll()` and `collapseAll()` functions and render mass action buttons above the fields list.
    4. Update card header to show:
       - Chevron expand/collapse indicator with rotation animation
       - Number badge (e.g. `#1`)
       - Field label (with fallback to field.name or 'Pertanyaan baru')
       - Type badge
       - Badges for 'Wajib', option count (e.g. '3 Opsi'), or quiz score
       - Action buttons with `e.stopPropagation()` so reordering/deleting doesn't toggle collapse
    5. Wrap card body in `.admin-builder-card-body` and render only when not collapsed (or display: none).
    6. When a new field is added via `addField(type)`, ensure it is not collapsed.
  </action>
  <acceptance_criteria>
    Clicking a card header toggles its open/closed state; clicking action buttons inside the header moves/deletes without toggling; closed cards display complete summary info; mass "Buka Semua" and "Tutup Semua" buttons open/close all visible cards.
  </acceptance_criteria>
</task>

<task id="3" name="verify_phase_15" type="auto">
  <read_first>
    <file>src/components/AdminFormEditor.tsx</file>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Run vitest tests (`npm test -- --run`), TypeScript compilation (`npx tsc --noEmit --pretty false`), and ESLint (`npm run lint`) to verify zero errors and maintain test coverage.
  </action>
  <acceptance_criteria>
    Tests pass, TypeScript compiler reports no type errors, ESLint passes with 0 warnings/errors.
  </acceptance_criteria>
</task>

## Verification
- Run `npm test -- --run`
- Run `npx tsc --noEmit --pretty false`
- Run `npm run lint`

## must_haves
- truths:
  - "User can collapse and expand individual field cards by clicking the card header"
  - "Clicking action buttons in header does not trigger expand/collapse toggle"
  - "Collapsed header displays question index, label, type, required badge, and option/score count"
  - "User can expand all and collapse all cards in one click"
- prohibitions:
  - "Do not introduce third-party accordion libraries"
