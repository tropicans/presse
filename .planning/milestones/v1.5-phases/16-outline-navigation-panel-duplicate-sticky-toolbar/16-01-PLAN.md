---
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/AdminFormEditor.tsx
autonomous: true
requirements:
  - BUILDER-06
  - BUILDER-07
  - BUILDER-08
  - BUILDER-10
---

# Plan 16-01: Outline Navigation Panel, 1-Click Duplicate & Sticky Toolbar Polish

## Goal
Implement an Outline Navigation Panel for jumping directly to any question across steps (BUILDER-06, BUILDER-07), 1-click field duplication with complete configuration cloning (BUILDER-08), and sticky toolbar polish for continuous ergonomics during scrolling (BUILDER-10).

## Tasks

<task id="1" name="css_outline_duplicate_and_sticky_toolbar" type="auto">
  <read_first>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Add CSS styles in src/app/globals.css for:
    - Outline panel container, collapsible toggle, step grouping, and question link items (`.admin-outline-panel`, `.admin-outline-item`, etc.).
    - Temporary visual highlight animation for targeted cards (`.admin-builder-card-highlight`).
    - Sticky toolbar enhancements with backdrop-filter, solid opaque background, and elevated z-index during scrolling.
    - Dark mode adaptations.
  </action>
  <acceptance_criteria>
    src/app/globals.css includes styles for `.admin-outline-panel`, `.admin-outline-item`, `.admin-builder-card-highlight`, and sticky toolbar rules for light and dark themes.
  </acceptance_criteria>
</task>

<task id="2" name="admin_form_editor_outline_and_duplicate" type="auto">
  <read_first>
    <file>src/components/AdminFormEditor.tsx</file>
  </read_first>
  <action>
    1. Implement `duplicateField(fieldId)` to clone a field, its type, required setting, placeholder, and all options, inserting the clone directly after the source field in `form.fields` on the same step.
    2. Add a Duplicate button (copy icon/tooltip) to the card actions row with `e.stopPropagation()`.
    3. Implement the Outline Navigation Panel:
       - Displays hierarchy of steps and questions with index numbers and question labels.
       - Clicking a question navigates to it: switches `activeStepTab` to that step if needed, expands the card if collapsed, scrolls viewport to the card with `scrollIntoView({ behavior: 'smooth', block: 'center' })`, and applies a momentary highlight effect.
       - Include a toggle button to show/hide the Outline panel.
    4. Ensure the field toolbar and topbar adhere to sticky behavior.
  </action>
  <acceptance_criteria>
    Clicking Outline item switches to the question's step, expands the card, and scrolls into view; clicking duplicate creates a complete copy directly below the target; toolbar remains accessible during long scrolls.
  </acceptance_criteria>
</task>

<task id="3" name="verify_phase_16" type="auto">
  <read_first>
    <file>src/components/AdminFormEditor.tsx</file>
    <file>src/app/globals.css</file>
  </read_first>
  <action>
    Run vitest tests (`npm test -- --run`), TypeScript compilation (`npx tsc --noEmit --pretty false`), and ESLint (`npm run lint`) to verify zero errors and maintain test coverage.
  </action>
  <acceptance_criteria>
    All vitest tests pass, TypeScript compiler reports 0 errors, ESLint passes with 0 warnings/errors.
  </acceptance_criteria>
</task>

## Verification
- Run `npm test -- --run`
- Run `npx tsc --noEmit --pretty false`
- Run `npm run lint`

## must_haves
- truths:
  - "User can view an outline of all steps and questions in the form"
  - "Clicking an outline item navigates to and auto-expands the corresponding field card"
  - "User can duplicate any field in 1 click, cloning all its configuration"
  - "Action header and field toolbar stay sticky during vertical scroll"
- prohibitions:
  - "Do not introduce third-party scroll or drawer libraries"
